// JavaScript disiapkan untuk pengembangan lanjutan
// Contoh: resize iframe otomatis (opsional)

window.addEventListener("load", () => {
  const iframe = document.getElementById("uploadFrame");
  console.log("Iframe loaded:", iframe.src);
});
